
#include <road.h>

void drawLine(){
     //cv::line(debug::mask, r.pointBot,r.pointTop,cv::Scalar(255,0,0),2);
}
//for(cv::Point p : layer.points){
//    cv::circle(layer.mask, p, 4, cv::Scalar(0,255,0), 3);
//}

//for(unsigned int i = 0; i < contours.size(); i++){
//    cv::drawContours(layer.mask, contours, i, cv::Scalar(255,255,255), 2, 1, std::vector<cv::Vec4i>(), 0, cv::Point() );
//}


//// debug
//if(isDebug){
//   for(Lane &lane : dst_lanes){
//       for(cv::Point &p : lane.cnt){
//           cv::circle(debug::mask, p, 5,cv::Scalar(0,255,255),-1);
//       }
//       cv::line(debug::mask, lane.line.start, lane.line.end, cv::Scalar(255,0,255), 2);
//   }
//}
